{
  "tasks": {
    "build": "rm -rf ./dist/ && cp -r ./public ./dist && rm ./dist/popup/popup.scss && deno run --allow-env --allow-read --allow-write bundle.ts"
  },
  "compilerOptions": {
    "lib": ["dom", "dom.iterable", "dom.asynciterable", "deno.ns"],
    "types": ["https://unpkg.com/chrome-types@latest/index.d.ts"]
  }
}
